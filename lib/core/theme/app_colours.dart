import 'package:flutter/material.dart';

class EduranceColors {
  // Primary brand color - used for main actions, buttons, and key UI elements
  static const Color primary = Color(0xFF26CCC2);
  
  // Secondary brand color - used for accents, hover states, and secondary actions
  static const Color secondary = Color(0xFF6AECE1);
  
  // Highlight color - used for important highlights, promotions, and attention-grabbing elements
  static const Color highlight = Color(0xFFFFF57E);
  
  // Accent color - used for call-to-action elements, notifications, and special features
  static const Color accent = Color(0xFFFFB76C);
  
  // Background color - main app background, screens and containers
  static const Color background = Color(0xFFFFFDF7);
  
  // Surface color - cards, dialogs, sheets, and elevated surfaces
  static const Color surface = Color(0xFFFFFFFF);
  
  // Primary text color - main body text, headings, and important labels
  static const Color textPrimary = Color(0xFF22324A);
  
  // Muted text color - secondary text, hints, placeholders, and disabled text
  static const Color textMuted = Color(0xFF7A869A);
  
  // Success color - positive feedback, completed actions, and success states
  static const Color success = Color(0xFF59D98E);
  
  // Error color - error messages, validation feedback, and destructive actions
  static const Color error = Color(0xFFFF6B6B);
}